import 'package:flutter/material.dart';

class category_listtile_trailing extends StatelessWidget {
  const category_listtile_trailing({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.arrow_forward_ios, size: 18, color: Colors.grey);
  }
}
